---
paths:
  - "vitest.config.mts"
  - "src/**/*.ts"
---

# Coverage

Never lower a threshold, and never add a `v8 ignore` comment to reach one; exclusion by file
name in `vitest.config.mts` with a stated reason is the only relief.
